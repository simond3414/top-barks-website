---
interface Props {
  variant?: "square" | "full";
  class?: string;
}

const { variant = "square", class: className = "" } = Astro.props;

const logoSrc = variant === "square" 
  ? "/images/logos/Square_Logo.png?v=2" 
  : "/images/logos/2026_Logo_V3_with_2006.png?v=2";
  
const altText = "Top Barks Dog Training - Established 2006";
---

<img 
  src={logoSrc} 
  alt={altText}
  width="40"
  height="40"
  class={className || "h-10 w-auto object-contain inline-block"}
  style="display: inline-block; min-width: 40px; min-height: 40px;"
/>
